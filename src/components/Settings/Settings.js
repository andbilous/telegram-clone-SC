import React from 'react';
import {ScrollView,FlatList,Text} from 'react-native';

const Settings = () => {
    return (
        <ScrollView>
            <Text>Settings </Text>
        </ScrollView>
    )
};

export  default  Settings;
